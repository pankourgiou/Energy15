//setting soad = 1 (soad is system of a down..)we have:
import Foundation

let bpm = 70.0
let beatDuration = 60.0 / bpm // seconds per beat

while true {
    print("E = soad*mc**2")
    fflush(stdout)
    Thread.sleep(forTimeInterval: beatDuration)
}
